name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - uses: actions/setup-go@v2
        with:
          go-version: "^1.17.0"

      # - name: Use Node.js
      #   uses: actions/setup-node@v1
      #   with:
      #       node-version: 15.8.0

      - run: ./build.sh
